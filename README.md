# riri_grape_leave_image_classification
build my own CNN model, used 6 other types of pre-trained models (eg. ResNet, EfficientNet, NasNet, etc.) for best model comparison. deployed the project on streamlit. 
